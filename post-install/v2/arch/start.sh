#!/bin/bash

# Styling variables
bold=$(tput bold)
underline=$(tput smul)
normal=$(tput sgr0)

echo "${bold}Arch-based distribution detected.${normal}"
echo "${bold}Executing Arch-specific commands...${normal}"